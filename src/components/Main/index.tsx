import { Box, Container } from "@chakra-ui/react";

function Main() {
  return <Container maxW={'container.lg'} bg={"pink"} centerContent>
    <Box w={'full'} color={'white'}>Main</Box>
  </Container>;
}

export default Main;
